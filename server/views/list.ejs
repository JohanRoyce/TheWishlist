<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  
  <link rel='stylesheet' href='/Content/app.css' />
</head>

<body>
  <h1><%= title %></h1>
    <p>Add or Update your xmas gift wishlist!</p>

    <div class ="container">
        <div class="row">
        <div class="col-md-offset-3 col-md-10">
        <h3><%= title %></h3>
        <a href = "/gifts/list/add" class="btn btn-primary" role="button">
            <i class="fas fa-plus-cirle"></i> Add a gift </a>
        <br>


        <br>
        <table class= "table table-bordered">
            <tr>
                <th class="text-center col-4">Name</th>
                <th class="text-center col-4">Retailer</th>
                <th class="text-center col-4">Price</th>
                <th class="text-center col-4">Discount</th>
                <th class="text-center col-4">Edit</th>
                <th class="text-center col-4">Delete</th>
            </tr>
            <% for(let count=0; count<giftlist.length; count++) { %>
                <tr>
                    <td class="text-center col-4"><%= giftlist[count].name %></td>
                    <td class="text-center col-4"><%= giftlist[count].retailer %></td>
                    <td class="text-center col-4"><%= giftlist[count].price %></td>
                    <td class="text-center col-4"><%= giftlist[count].discount %></td>
                    <td class="text-center col-4">
                        <a href="/gift-list/edit/<%= giftlist[count].id %>"
                            class="btn btn-primary"
                                <i class="fas fa-pencil-alt"></i> Edit </a>
                    </td>
                    <td class="text-center col-4">
                        <a href="/gift-list/delete/<%= giftlist[count].id %>"
                            class="btn btn-danger"
                                <i class="fas fa-trash-alt"></i> Edit </a>
                    </td>
                </tr>
            <% } %>
        </table>
        </div>
        </div>
    </div>
    
    <script src = "/Scripts/app.js"></script>
</body>
</html>