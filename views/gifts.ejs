<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  
  <link rel='stylesheet' href='/Content/app.css' />
</head>

<body>
  <h1><%= title %></h1>
    <p>Add or Update your xmas gift wishlist!</p>

    <div class ="container">
        <div class="row">
        <div class="col-md-offset-3 col-md-6">
        <h3><%= title %></h3>
        <br>
        <table class= "table table-bordered">
            <tr>
                <th class="text-center">Name</th>
                <th class="text-center">Retailer</th>
                <th class="text-center">Price</th>
                <th class="text-center">Discount</th>
            </tr>
            <% for(let count=0; count<giftlist.length; count++) { %>
                <tr>
                    <td class="text-center"><%= giftlist[count].name %></td>
                    <td class="text-center"><%= giftlist[count].retailer %></td>
                    <td class="text-center"><%= giftlist[count].price %></td>
                    <td class="text-center"><%= giftlist[count].discount %></td>
                </tr>
            <% } %>
        </table>
        </div>
        </div>
    </div>
    
    <script src = "/Scripts/app.js"></script>
</body>
</html>